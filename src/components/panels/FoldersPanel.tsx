import React from 'react';
import { PanelHeader } from '@vkontakte/vkui';

const FoldersPanel = () => {
  return (
    <>
      <PanelHeader>Папки</PanelHeader>
    </>
  );
};

export default React.memo(FoldersPanel);
