import React from 'react';
import { PanelHeader } from '@vkontakte/vkui';

const ArticlesPanel = () => {
  return (
    <>
      <PanelHeader>Статьи</PanelHeader>
    </>
  );
};

export default React.memo(ArticlesPanel);
