mutation createCollection($params: CreateCollectionParamsInput!) {
  createCollection(params: $params) {
    id
    title
    description
    ownerId
    bookmarks {
      id
      link
      title
      description
      ownerId
      collectionId
      createdAt
    }
    createdAt
  }
}
